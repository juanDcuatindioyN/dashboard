# PostgreSQL connection string (same DB as academic-record)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Server port — must match the dashboard's API_URL (localhost:3003)
PORT=3003

# Environment: development | production
NODE_ENV=development
